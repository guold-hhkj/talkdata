<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hh.android.hhstats">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".StatsApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Test1Activity"/>
        <activity android:name=".Test2Activity"/>
        <activity android:name=".Test3Activity"/>
        <activity android:name=".Test4Activity"/>

        <meta-data
            android:name="TALK_DATA_CHANNEL"
            android:value="huawei">
        </meta-data>

        <meta-data
            android:name="APP_KEY"
            android:value="56fc83d7e0f55a51cf00008d">
        </meta-data>

    </application>

</manifest>